<%- include("partials/head") %>
<%- include("partials/header") %>

<section class="panel">
  <p class="eyebrow">About</p>
  <h2>What I am learning here</h2>
  <p>This assignment is mostly about organizing code with MVC, using PostgreSQL instead of memory arrays, and keeping login state in sessions that survive restarts.</p>
  <p>I tried to keep the logic pretty direct so it is easier to follow while still being safe with SQL parameter placeholders and server-side validation.</p>
</section>

<%- include("partials/footer") %>
